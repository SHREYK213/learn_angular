import { AlphabeticOnlyDirective } from './alphabetic-only.directive';

describe('AlphabeticOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabeticOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
